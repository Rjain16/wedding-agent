face_recognition==1.3.0
Pillow>=10.0.0
gdown>=5.1.0
tqdm>=4.66.0
numpy>=1.24.0
# cmake and dlib are required by face_recognition:
# brew install cmake (macOS) or apt install cmake (Linux)
# pip install dlib  (or conda install -c conda-forge dlib)
